<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Your profile</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
      <h3 class="text-3xl">
        <span class="font-light">Hello</span>, <%= user.name %>👋
      </h3>
      <h5 class="mb-5">You can create a new post</h5>
      <form action="">
        <textarea
          name=""
          placeholder="what's on your mind"
          class="p-3 outline-none w-1/3 resize-non bg-transparent border-2 border-zinc-800 rounded-md block"
        ></textarea>
        <input
          class="px-3 w-40 py-2 bg-blue-500 block rounded-md mt-2 text-sm"
          type="submit"
          value="Create New Post"
        />
      </form>
    </div>
  </body>
</html>
